import Pelicula from "@/models/interface_pelicula";

export interface Props {
  llamada: string;
  pelicula: Pelicula;
  estilo: number;
}

export interface Props1 {
  llamada: string;
  pelicula: Pelicula;
}
